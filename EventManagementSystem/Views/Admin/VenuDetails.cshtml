@model IEnumerable<EventManagementSystem.Models.AutorizedUser.VenuDetails>

@{
    ViewBag.Title = "VenuDetails";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>VenuDetails</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VenuName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GuestCapacity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VenuName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GuestCapacity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VenuId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VenuId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VenuId })
        </td>
    </tr>
}

</table>
